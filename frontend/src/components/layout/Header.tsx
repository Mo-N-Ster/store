import{useEffect,useState}from'react';import type{User}from'../../types';
type Props={user:User;title:string;onMode:()=>void;onLogout:()=>void;theme:string;setTheme:(v:string)=>void;lang:string;setLang:(v:string)=>void};
export function Header({user,title,onMode,onLogout,theme,setTheme,lang,setLang}:Props){const[clock,setClock]=useState(new Date());useEffect(()=>{const id=setInterval(()=>setClock(new Date()),1000);return()=>clearInterval(id)},[]);return <header><h2>{title}</h2><time>{clock.toLocaleString()}</time><span className="avatar">{user.initials}</span>{user.role==='admin'&&<button className="ghost" onClick={onMode}>⇄</button>}<button className="ghost" onClick={()=>setLang(lang==='fr'?'en':'fr')}>{lang.toUpperCase()}</button><button className="ghost" onClick={()=>setTheme(theme==='light'?'dark':'light')}>{theme==='light'?'☾':'☀'}</button><button className="danger ghost" onClick={onLogout}>⏻</button></header>}
