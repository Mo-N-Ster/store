import{dialog,ipcMain}from'electron';import{api}from'../database/storeDatabase.js';import{IPC_PREFIX}from'../ipc/channels.js';
export function registerIpcHandlers(){for(const[name,handler]of Object.entries(api))ipcMain.handle(`${IPC_PREFIX}${name}`,(_event,...args)=>(handler as(...values:unknown[])=>unknown)(...args));ipcMain.handle(`${IPC_PREFIX}saveExport`,async(_event,{name,content}:{name:string;content:string})=>{const result=await dialog.showSaveDialog({defaultPath:name});if(result.canceled||!result.filePath)return null;await import('node:fs/promises').then(fs=>fs.writeFile(result.filePath!,content,'utf8'));return result.filePath})}
