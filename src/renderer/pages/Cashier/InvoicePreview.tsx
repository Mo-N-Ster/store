import{useTranslation}from'react-i18next';import{formatMoney}from'../../utils/formatters';
export function InvoicePreview({data,close}:{data:any;close:()=>void}){const{t}=useTranslation();const invoice=data.invoice;return <div className="modal"><section className="receipt"><div className="receipt-brand">STORE</div><h2>{t('invoice')} {invoice.id}</h2><p>{new Date(invoice.invoice_date).toLocaleString()} · {invoice.seller} ({invoice.initials})</p><table><tbody>{data.lines.map((line:any)=><tr key={line.id}><td>{line.product_name}</td><td>{line.quantity} × {formatMoney(line.unit_price)}</td><td>{formatMoney(line.total_line)}</td></tr>)}</tbody></table><p>Remise: {formatMoney(invoice.discount)}</p><h2>{t('total')}: {formatMoney(invoice.total_amount)}</h2><div><button onClick={()=>window.print()}>{t('print')}</button><button className="ghost" onClick={close}>{t('close')}</button></div></section></div>}
