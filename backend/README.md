# Backend

Processus Electron, preload, IPC et persistance SQLite. Les règles pures et validations résident dans `src/domain`; les transactions restent dans la couche database afin de préserver leur atomicité.
